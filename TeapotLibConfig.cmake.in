
@PACKAGE_INIT@

include ( "${CMAKE_CURRENT_LIST_DIR}/TeapotLibTargets.cmake" )

include(CMakeFindDependencyMacro)

list(APPEND CMAKE_PREFIX_PATH "C:/_libraries/GLFW/lib/cmake/glfw3")

find_dependency(Vulkan REQUIRED)
find_dependency(glfw3 REQUIRED CONFIG)

include ( "${CMAKE_CURRENT_LIST_DIR}/TeapotLibTargets.cmake" )

message(STATUS "TeapotLib found successfully")
